# Local GPU install only. Skip on Google Colab (already ships CUDA-enabled torch).
# The cu121 wheels work with any CUDA 12.x driver (12.1 through 12.6 tested).
# For CPU-only local use, replace with:
#     pip install torch==2.3.1 torchvision==0.18.1

--extra-index-url https://download.pytorch.org/whl/cu121

torch==2.3.1+cu121
torchvision==0.18.1+cu121
